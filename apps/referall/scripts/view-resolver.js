
(function() {
  'use strict';
  // Gets view name and params from router, and resolves view with params
  var resolve = function (params) {

  };

  var ViewResolver = function(router){};
  window.referall.ViewResolver = ViewResolver;

}).call(this);